import "reflect-metadata";
import "source-map-support/register";
import { Clock, Logger, Time } from "../../../lib/metrics/";
export declare class MockedLogger implements Logger {
    calls: any[];
    log(): void;
    error(): void;
    warn(): void;
    info(msg: string, metadata: any): void;
    debug(): void;
    trace(): void;
}
export declare class MockedClock implements Clock {
    private currentTime;
    time(): Time;
    setCurrentTime(time: Time): void;
}
export declare class LoggerReporterTest {
    private clock;
    private registry;
    private logger;
    private internalCallback;
    private scheduler;
    private schedulerSpy;
    private reporter;
    before(): void;
    "report multiple metric with same name"(): Promise<void>;
}
