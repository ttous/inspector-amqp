import "reflect-metadata";
import "source-map-support/register";
export declare class DefaultReservoirTest {
    "check correct size"(): void;
    "check snapshot from no values"(): void;
    "check snapshot from one value"(): void;
    "check snapshot from same value twice"(): void;
    "check snapshot from same value twice with fluent interface"(): void;
    "check snapshot from different values"(): void;
    "check snapshot from same value more times than capacity"(): void;
    "check snapshot from same value more times than capacity with fluent interface"(): void;
    "check snapshot from different values - overloading capacity"(): void;
}
export declare class SlidingWindowReservoirTest {
    "check correct size"(): void;
    "check correct size with fluent interface"(): void;
    "check snapshot from no values"(): void;
    "check snapshot from one value"(): void;
    "check snapshot from same value twice"(): void;
    "check snapshot from same value twice with fluent interface"(): void;
    "check snapshot from different values"(): void;
    "check snapshot from same value more times than capacity"(): void;
    "check snapshot from same value more times than capacity with fluent interface"(): void;
    "check snapshot from different values - overloading capacity"(): void;
}
