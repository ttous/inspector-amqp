import "reflect-metadata";
import "source-map-support/register";
export declare class DiffTest {
    "diff with null values"(): void;
    "diff with same value"(): void;
    "diff with different nanoseconds"(): void;
    "diff with different milliseconds"(): void;
    "diff with different milliseconds and nanoseconds"(): void;
}
export declare class StdClockTest {
    "check time function"(): void;
}
