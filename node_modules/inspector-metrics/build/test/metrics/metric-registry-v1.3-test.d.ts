import "reflect-metadata";
import "source-map-support/register";
export declare class MetricRegistryTest {
    "register multiple metrics with same name"(): void;
    "register metrics-set within metrics-registry"(): void;
}
